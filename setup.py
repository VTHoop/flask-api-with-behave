__author__ = 'hooper-p'

# from distutils.core import setup
from setuptools import setup
setup(name='BikeProjectWithFlask',
      version='1.0',
      description='Simple API project with Flask',
      packages=[
            'src'
      ],
     )
